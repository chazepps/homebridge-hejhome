# Design Docs

This directory is reserved for focused design decisions that are larger than a source comment but smaller than the root architecture document.

Current architecture notes live under `../project-avatar/architectures/`.
